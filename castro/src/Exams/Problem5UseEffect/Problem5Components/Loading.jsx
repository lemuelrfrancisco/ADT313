function Loading() {
  return (
    <div
      style={{
        background: 'darkblue',
        height: '100%',
        width: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <h2>Page is loading...</h2>
    </div>
  );
}

export default Loading;
